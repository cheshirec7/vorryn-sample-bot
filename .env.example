# Shared bearer token that Vorryn sends on every /play request. Match this
# to the "Shared secret" you configure on /profile/bots.
# Generate one with: openssl rand -base64 32
BOT_BEARER=replace-me-with-a-strong-random-string

# Port the bot listens on. Default 3001.
PORT=3001
